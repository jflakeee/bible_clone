# 구현 결과물 재검토 ? 결정 체크리스트

> 작성일: 2026-03-11
> 목적: 설계 vs 구현 차이점에 대해 **유지/수정/삭제** 방향을 결정

---

## 1. 아키텍처 결정

### 1.1 백엔드 구조
- [ ] **NestJS 백엔드 서버** ? 현재 클라이언트 직접 호출 유지

- [ ] **Python FastAPI AI 서버** ? AI 기능 보류

### 1.2 데이터베이스
- [ ] **Supabase PostgreSQL 연동** ? localStorage 유지

- [ ] **Meilisearch 검색 엔진** ? 현재 클라이언트 순회 방식 유지

- [ ] **Redis 캐시** ? 불필요

### 1.3 배포/인프라
- [ ] **모노레포 구조** ? 현재 단일 프로젝트 유지

- [ ] **Docker/CI/CD** ? 현재 정적 빌드 유지

---

## 2. 기능 범위 결정

### 2.1 인증/사용자
- [ ] **사용자 인증** ? 인증 없이 로컬 전용

- [ ] **결제/구독** ? 향후 구현으로 보류

### 2.2 오프라인/모바일
- [ ] **오프라인 지원** ? SW + 오프라인 캐시

- [ ] **React Native 모바일 앱** ? 향후 보류

### 2.3 AI/TTS
- [ ] **Google Cloud TTS** ? 삭제

- [ ] **Bible Brain API** ? 삭제

- [ ] **벡터 검색 (pgvector)** ? 향후 보류

---

## 3. 더미데이터 처리 결정

### 3.1 설교 데이터 (40개 샘플)
- [ ] **SAMPLE_SERMONS** 향후 보류

### 3.2 고유명사 데이터 (200+항목)
- [ ] **PROPER_NOUNS** 현재 TS 파일 유지

### 3.3 지도 데이터 (60+지명)
- [ ] **BIBLICAL_PLACES** 현재 TS 파일 유지

### 3.4 히브리어 원문 샘플
- [ ] **HEBREW_SAMPLE_DATA** 현재 2개 장 유지

### 3.5 오늘의 말씀
- [ ] **DAILY_VERSES** 현재 20개 유지

---

## 4. 중복 코드 처리 결정

- [ ] **`getLanguageForBook()`** ? 하나로 통합

- [ ] **`getAvailableTranslations()`** ? 하나로 통합

- [ ] **TTS 유틸 함수** ? 클래스로 통합, standalone 삭제

- [ ] **History Store** ? 하나로 통합

- [ ] **`API_BASE` 상수** ? `constants.ts`로 통합

---

## 5. 오버스펙 처리 결정

- [ ] **TTS 3종 팩토리 패턴** ? Browser만 남기고 나머지 삭제

- [ ] **Google TTS voice 목록 하드코딩** ? 삭제

- [ ] **Supabase 클라이언트** ? 연결 전까지 삭제

- [ ] **설교 추천 알고리즘** ? 현재 유지

---

## 6. 하드코딩 처리 결정

- [ ] **외부 API URL 4곳** ? 전부 `.env`로

- [ ] **언어 매핑 (`LANGUAGE_KO_MAP`)** ? i18n 파일로 분리

---

## 7. 라우트 구조 결정

- [ ] **`/[...params]`** 현재 catch-all 유지

- [ ] **`/(bible)/strongs`** vs 설계의 **` 현재 bible 그룹 유지

---

## 우선순위 제안 (참고용)

| 우선순위 | 항목 | 이유 |
|---------|------|------|
| **긴급** | 검색 방식 개선 (2.1.2) | 현재 방식은 프로덕션 불가 |
| **높음** | 중복 코드 정리 (4.전체) | 유지보수 비용 절감 |
| **높음** | 더미 설교 데이터 방향 (3.1) | 가상 목사명 등 품질 이슈 |
| **중간** | 히브리어 원문 확장 (3.4) | 구약 39권 중 2장만 제공 |
| **중간** | 오버스펙 정리 (5.전체) | 코드 복잡도 감소 |
| **낮음** | 하드코딩 환경변수화 (6.전체) | 현재 값이 변경될 가능성 낮음 |
| **보류** | 모바일 앱, 결제 (2.1, 2.2) | MVP 범위 외 |
